import { BoolformatPipe } from './boolformat.pipe';

describe('BoolformatPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolformatPipe();
    expect(pipe).toBeTruthy();
  });
});
